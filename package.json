{
  "name": "buja-auto-spa-erp",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first ERP for Buja Auto Spa - Truck parts, maintenance, EV & truck rentals, car wash",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && npm install && npx prisma generate && npx prisma migrate dev --name init && npm run db:seed",
    "setup:frontend": "cd frontend && npm install",
    "test:build": "npm run build && echo '✅ Build successful - Foundation verified'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
